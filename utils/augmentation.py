import tensorflow as tf


def random_augmentation(img, bbox, mask):
    """
    # Photometric Distortions on image
    # Geometric Distortions on image, bboxes and mask
    :param img:
    :param bbox:
    :param mask:
    :return:
    """
    return img, bbox, mask
